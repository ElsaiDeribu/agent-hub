---
title: Hello World
description: Minimal hello template previewed in the sandbox (no API keys required).
---

<AgentPlayground name="hello" />

## Overview

The smallest registry template. Use it to confirm scaffolding, sandbox streaming, and the preview contract before you adopt a fuller agent.

## How it works

A generic `agent.stream()` implementation echoes a short greeting. There are no tools, no model calls, and no environment variables.

## Environment

None. The sandbox preview runs without API keys.
