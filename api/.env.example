# Copy to .env and fill in values:
#   cp .env.example .env
#
# Values below are for Docker Compose. Use a different .env in production.

# --- Postgres ---
# Host `postgres` is the Compose service name. USER/PASSWORD/DB must match
# the postgres service environment in docker-compose.yml (that image is
# configured there, not via this file). config.py builds DATABASE_URL from these.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=agenthub
POSTGRES_USER=agenthub
POSTGRES_PASSWORD=agenthub

# --- Auth ---
# Public base URL of the auth router (no trailing slash).
# Google OAuth redirect URI is derived as: ${AUTH_BASE_URL}/oauth/callback/google
AUTH_BASE_URL=http://localhost:8000/api/auth

# Where to send the browser after OAuth / sign-in (frontend).
AUTH_FRONTEND_CALLBACK=http://localhost:3000

# Session cookie lifetime in minutes (default: 7 days).
# AUTH_SESSION_EXPIRES_MINUTES=10080

# Set to true in production (HTTPS) so the session cookie is not sent over HTTP.
# AUTH_COOKIE_SECURE=true

# --- Google OAuth ---
# Optional. Leave empty to disable Google sign-in.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# GOOGLE_AUTHORIZATION_URL=https://accounts.google.com/o/oauth2/v2/auth
# GOOGLE_TOKEN_URL=https://oauth2.googleapis.com/token
# GOOGLE_USERINFO_URL=https://www.googleapis.com/oauth2/v2/userinfo
# GOOGLE_SCOPES=openid email profile

# --- HTTP / CORS ---
# Comma-separated browser origins allowed to call the API with credentials.
CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# Bind address/port when running uvicorn directly (Compose start script uses 0.0.0.0:8000).
# HOST=0.0.0.0
# PORT=8000

# --- Sandbox / registry ---
REGISTRY_DIR=/app/registry
# MSB_IMAGE=node
# SESSION_IDLE_TIMEOUT=1800
# SESSION_MAX_DURATION=3600
# SESSION_BASE_PORT=10000
