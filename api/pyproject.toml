[project]
name = "microsandbox-demo"
version = "0.2.0"
description = "Preview AI agents inside microsandbox microVMs via FastAPI."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
    "httpx>=0.28.0",
    "microsandbox>=0.6.6",
    "uvicorn>=0.51.0",
]
